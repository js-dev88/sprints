jQuery(document).ready(function(){

	/*$("#submitConnection").click(function(){
		
		$.post(
			'?r=t_e_client_cli/formConnectAction',
			{	
				 email : $("#connectionEmail").val(),
				 password : $("#connectionMdp").val()

			},
			function(donnee){

                if(donnee == 'Success'){
                     // Le membre est connecté. Ajoutons lui un message dans la page HTML.

                     $("#msgError").html("<p>Vous avez été connecté avec succès !</p>");
                }
                else{
                     // Le membre n'a pas été connecté. (data vaut ici "failed")

                     $("#msgError").html("<p>Erreur lors de la connexion...</p>");
                }
			},
			'text'
		);
	});*/
});